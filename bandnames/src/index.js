import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import { BandNames } from './band-names-app';

ReactDOM.render(
  <BandNames />,
  document.getElementById('root')
);